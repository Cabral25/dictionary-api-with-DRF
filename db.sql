/* CREATE DATABASE dictionary_api_with_DRF; */

/* CREATE TABLE users(
    user_id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    first_name,
    last_name,
    is_staff,
    is_superuser,
    is_active,
    last_login,
    date_joined,
) */